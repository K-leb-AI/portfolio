import React from "react";

const LeftBlob = (props) => {
  return (
    <div
      className={`absolute w-20 aspect-square opacity-45 bg-purple-500 rounded-full right-0 backdrop-blur-xl blur-3xl`}
    ></div>
  );
};

export default LeftBlob;
